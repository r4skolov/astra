/*!
 * External js:
 * MoveTo https://github.com/hsnaydd/moveTo MIT
 * lite-youtube-embed https://github.com/paulirish/lite-youtube-embed MIT
 */
/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
// import { gsap }                from 'gsap';
// import { TweenMax, TweenLite } from 'gsap/gsap-core';
//
// gsap.registerPlugin(TweenMax);
//
// let mouseX = 0;
// let mouseY = 0;
//
// window.addEventListener('mousemove', (e) => {
// 	const hero         = document.querySelector('.hero');
// 	const parallaxItem = document.querySelector('.hero-image__pic');
// 	const pageX        = event.pageX - (hero.getBoundingClientRect().width * 0.5);
// 	const pageY        = event.pageY - (hero.getBoundingClientRect().height * 0.5);
// 	const fixer        = 0.0010;
//
// 	mouseX = e.clientX;
// 	mouseY = e.clientY;
//
// 	const speedX = parallaxItem.getAttribute('data-speed-x');
// 	const speedY = parallaxItem.getAttribute('data-speed-y');
//
// 	TweenLite.set(parallaxItem, {
// 		x : (parallaxItem.offsetLeft + pageX * speedX) * fixer,
// 		y : (parallaxItem.offsetTop + pageY * speedY) * fixer,
// 	});
// });
/******/ })()
;
//# sourceMappingURL=gsap.js.map